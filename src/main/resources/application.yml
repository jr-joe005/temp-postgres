# 端口
server:
  port: 8081

# DB配置
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db?ssl=true&sslmode=require&sslcert=/ssl_file/server.crt&&sslkey=/ssl_file/server.key
    username: testuser
    password: testpassword
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        hbm2ddl:
#          auto: update

